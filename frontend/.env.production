# The backend is deployed on the same domain as this frontend, reverse-proxied under
# /api (see the Nginx/Apache config on the Hostinger VPS) — so requests need no absolute
# host at all, just the relative /api path that client.ts already builds by default.
VITE_API_BASE_URL=
